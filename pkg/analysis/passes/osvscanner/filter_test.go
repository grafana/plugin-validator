package osvscanner

import (
	"testing"
)

func TestFilterPackages(t *testing.T) {
	/*
		packages := models.VulnerabilityResults{
			"d3-color",
			"moment",
		}

		filteredResults := FilterOSVInternalResults(packages)
		require.Len(t, filteredResults, 1) // should not have moment
		require.Equal(t, "d3-color", filteredResults[0])
	*/
}
