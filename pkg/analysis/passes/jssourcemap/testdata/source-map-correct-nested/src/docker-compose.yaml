version: '3.0'

services:
  grafana:
    container_name: 'grafanatestonly-nestedplugin-app'
    build:
      context: ./.config
      args:
        grafana_version: ${GRAFANA_VERSION:-9.2.5}
    ports:
      - 3000:3000/tcp
    volumes:
      - ./dist:/var/lib/grafana/plugins/grafanatestonly-nestedplugin-app
      - ./provisioning:/etc/grafana/provisioning
